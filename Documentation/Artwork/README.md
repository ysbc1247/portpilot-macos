# PortPilot artwork

`PortPilotIconSource.png` is the high-resolution square master app-icon artwork generated with OpenAI's built-in image generation tool. The icon combines a harbor pilot light with three connected network listeners. It uses a restrained navy, cyan, and amber palette designed to remain legible at small macOS icon sizes.

Regenerate asset-catalog sizes with:

```bash
Scripts/generate_app_icons.sh
```

Final generation prompt: create a premium macOS icon for PortPilot with a centered harbor/port pilot-light motif, three connected network nodes, and a subtle routing path; use crisp dimensional vector-like geometry, a dark navy and graphite base, cyan/teal connections, and one amber status accent; keep it edge-to-edge opaque with no text, numbers, third-party logos, watermark, or white outer corners.
